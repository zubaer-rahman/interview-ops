const e={id:"nextjs-ssr",title:"Server-Side Rendering",difficulty:"intermediate",estimatedMinutes:25,tldr:["Server-Side Rendering (SSR) generates the full HTML for a page on the server for each request, sending a fully-rendered page to the client.",'In Next.js, SSR is achieved via getServerSideProps in the Pages Router or by using dynamic = "force-dynamic" in the App Router.',"SSR improves SEO by providing fully-rendered HTML to search engine crawlers and reduces time-to-first-contentful-paint.","Trade-offs include higher server load, longer response times under high traffic, and no ability to cache the full HTML across users without additional configuration."],laymanDefinition:"SSR is like a restaurant that cooks your meal fresh every time you order. The kitchen (server) prepares the entire dish (HTML page) from scratch when you place your order (request), ensuring it is always hot and customized.",deepDive:[{heading:"How SSR Works",text:"When a request hits a Next.js server for an SSR page, the server executes getServerSideProps (Pages Router) or the async Server Component (App Router) on each request. Data is fetched, React renders the page to HTML on the server, and the fully-rendered HTML is sent to the client. The client then hydrates the HTML to make it interactive."},{heading:"SSR in the Pages Router vs App Router",text:'In the Pages Router, SSR is explicit via getServerSideProps which runs on every request. In the App Router, SSR is implicit: Server Components render on the server by default, and you opt into static rendering. To force SSR, you use dynamic = "force-dynamic" export, or use cookies(), headers(), or searchParams() which opt the segment into dynamic rendering.'},{heading:"Performance Considerations",text:"SSR pages have higher server response times because HTML is generated per request. Use streaming (App Router) to send HTML progressively. Implement CDN caching with s-maxage and stale-while-revalidate headers for non-personalized content. Monitor server CPU load as complex pages increase rendering time."},{heading:"SEO Benefits of SSR",text:"Search engine crawlers receive fully-rendered HTML, ensuring all content is indexed. SSR eliminates the need for Googlebot to execute JavaScript, which can be unreliable. Dynamic meta tags, Open Graph data, and structured data are all present in the initial HTML response, improving social sharing and search result snippets."},{heading:"Hydration and Interactivity",text:"After SSR HTML is delivered, React hydrates the page on the client, attaching event handlers and making the page interactive. Hydration must match the server-rendered HTML exactly; mismatches cause errors. The App Router improves on this with selective hydration and progressive enhancement."}],interviewAnswer:"SSR remains essential for applications requiring SEO with dynamic content, real-time data, or personalized content. Next.js makes SSR straightforward while offering performance optimizations like streaming and selective hydration. The choice between SSR, SSG, and ISR depends on data freshness requirements and traffic patterns.",interviewQuestions:[{question:"What is Server-Side Rendering in Next.js?",answer:"SSR generates the complete HTML for a page on the server for each incoming request. The server fetches data, executes React components, and produces full HTML that is sent to the client. The client then hydrates the HTML to make it interactive. SSR provides SEO benefits and faster initial content display."},{question:"How do you implement SSR in the Pages Router?",answer:"Export an async getServerSideProps function from the page component. This function runs on every request, receives the context object (params, req, res, query, etc.), and returns props that are passed to the page component at render time."},{question:"How do you implement SSR in the App Router?",answer:'In the App Router, Server Components render on the server by default. To ensure dynamic rendering (SSR-like behavior) on every request, export const dynamic = "force-dynamic" from the segment, or use cookies(), headers(), or searchParams() which automatically opt into dynamic rendering. No explicit data fetching function is needed.'},{question:"What are the performance implications of SSR?",answer:"SSR increases server response time because HTML is generated per request. This increases server CPU usage. Benefits include faster time-to-content for users and better SEO. Mitigations include HTTP caching headers (s-maxage), CDN caching, React streaming (App Router), and selective data fetching to minimize blocking."},{question:"How does SSR affect SEO?",answer:"SSR benefits SEO because search engine crawlers receive fully-rendered HTML with all content present. This ensures content is indexed even if crawlers do not execute JavaScript. Meta tags, Open Graph data, and structured data are all included in the initial server response."},{question:"What is hydration in the context of SSR?",answer:"Hydration is the process where React attaches event listeners and state to the server-rendered HTML on the client. It makes the static HTML interactive. React expects the client-side render tree to match the server-rendered HTML exactly; differences cause hydration errors."},{question:"How does caching work with SSR pages?",answer:"SSR pages can be cached using HTTP response headers. Set Cache-Control: s-maxage=60, stale-while-revalidate=300 for CDN caching. Avoid caching personalized content. Next.js does not cache SSR pages by default; caching must be configured in the hosting platform or via custom server."},{question:"What is the difference between SSR and SSG?",answer:"SSR generates HTML on every request (dynamic). SSG generates HTML once at build time (static). SSR is better for frequently changing data. SSG is better for content that does not change often and benefits from fast load times. ISR bridges the gap by allowing periodic revalidation of static pages."},{question:"Can you use SSR with getStaticProps?",answer:"No, getStaticProps is for SSG/ISR. A page can export either getServerSideProps (SSR) or getStaticProps (SSG/ISR), not both. Trying to export both will cause a build error."},{question:"What happens if getServerSideProps throws an error?",answer:"If getServerSideProps throws an error, Next.js shows a 500 error page in production. In development, it shows the error overlay. Use try-catch blocks in getServerSideProps to handle errors gracefully and return a notFound or redirect object instead of crashing."}],diagramSvg:'<svg viewBox="0 0 500 200" xmlns="http://www.w3.org/2000/svg" style="max-width:100%;height:auto;font-family:sans-serif"><rect x="0" y="0" width="500" height="200" rx="8" fill="#f8f9fa" stroke="#dee2e6" stroke-width="1"/><text x="250" y="24" text-anchor="middle" font-size="14" font-weight="bold" fill="#333">Server-Side Rendering</text><rect x="10" y="40" width="120" height="40" rx="4" fill="#0070f3" stroke="#0070f3" stroke-width="1"/><text x="70" y="56" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Client Request</text><text x="70" y="68" text-anchor="middle" font-size="9" fill="#ddd">Browser</text><line x1="130" y1="60" x2="150" y2="60" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="160" y="40" width="120" height="40" rx="4" fill="#28a745" stroke="#28a745" stroke-width="1"/><text x="220" y="56" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">getServerSideProps</text><text x="220" y="68" text-anchor="middle" font-size="9" fill="#ddd">Fetch Data</text><line x1="280" y1="60" x2="300" y2="60" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="310" y="40" width="120" height="40" rx="4" fill="#ffc107" stroke="#ffc107" stroke-width="1"/><text x="370" y="56" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Render to HTML</text><text x="370" y="68" text-anchor="middle" font-size="9" fill="#ddd">Server</text><line x1="430" y1="60" x2="450" y2="60" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="10" y="110" width="120" height="40" rx="4" fill="#17a2b8" stroke="#17a2b8" stroke-width="1"/><text x="70" y="126" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">HTML Response</text><text x="70" y="138" text-anchor="middle" font-size="9" fill="#ddd">to Client</text><line x1="130" y1="130" x2="160" y2="130" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="170" y="110" width="120" height="40" rx="4" fill="#6610f2" stroke="#6610f2" stroke-width="1"/><text x="230" y="126" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Hydration</text><text x="230" y="138" text-anchor="middle" font-size="9" fill="#ddd">JS Bundle</text><text x="250" y="180" font-size="9" fill="#666" text-anchor="middle">SSR: Full HTML generated server-side per request, then hydrated client-side.</text></svg>',codeExamples:[{title:"SSR with getServerSideProps",useCase:"When you need fresh data on every request.",code:`export async function getServerSideProps(context) {
  const { params, req, query } = context;
  const data = await fetch(\`https://api.example.com/data\`).then(r => r.json());
  return { props: { data } };
}

export default function Page({ data }) {
  return <div>{data.title}</div>
}`,description:"Fetches fresh data on every request and passes it as props to the page. The context object provides access to params, query, req, res, and preview data."},{title:"SSR in App Router",useCase:"When you need dynamic rendering in the App Router.",code:`// app/page.js
export const dynamic = "force-dynamic";

export default async function Page() {
  const data = await fetch("https://api.example.com/data");
  const json = await data.json();
  return <div>{json.title}</div>
}`,description:"The dynamic export forces server-side rendering on every request. The async component fetches data directly."},{title:"SSR with Streaming",useCase:"When you want to improve perceived performance by streaming HTML.",code:`// app/page.js
import { Suspense } from "react";

async function SlowComponent() {
  const data = await fetch("https://api.example.com/slow");
  return <div>{data}</div>
}

export default function Page() {
  return (
    <div>
      <h1>Fast header</h1>
      <Suspense fallback={<div>Loading...</div>}>
        <SlowComponent />
      </Suspense>
    </div>
  );
}`,description:"The header renders immediately while the slow component streams in later, improving perceived performance."},{title:"SSR with Custom Cache Headers",useCase:"When you want to balance freshness with performance via CDN caching.",code:`export async function getServerSideProps({ res }) {
  res.setHeader(
    "Cache-Control",
    "public, s-maxage=60, stale-while-revalidate=300"
  );
  const data = await fetch("https://api.example.com/data");
  const json = await data.json();
  return { props: { data: json } };
}`,description:"Sets HTTP cache headers for CDN caching: 60 seconds fresh, 300 seconds stale-while-revalidate."},{title:"SSR with Redirect",useCase:"When you need to redirect based on authentication or conditions.",code:`export async function getServerSideProps(context) {
  const token = context.req.cookies.token;
  if (!token) {
    return { redirect: { destination: "/login", permanent: false } };
  }
  const data = await fetch("https://api.example.com/data", {
    headers: { Authorization: \`Bearer \${token}\` }
  }).then(r => r.json());
  return { props: { data } };
}`,description:"Redirects unauthenticated users to /login before fetching data."}],mcqQuestions:[{question:"Which function implements SSR in the Pages Router?",options:["getStaticProps","getServerSideProps","getInitialProps","getStaticPaths"],answer:1,explanation:"getServerSideProps runs on every request to enable SSR."},{question:"How do you force dynamic rendering in the App Router?",options:['export const dynamic = "static"','export const dynamic = "force-dynamic"',"export const ssr = true",'export const render = "server"'],answer:1,explanation:'export const dynamic = "force-dynamic" ensures the page renders on every request.'},{question:"What is the main SEO benefit of SSR?",options:["Smaller bundle size","Fully-rendered HTML for crawlers","Faster client navigation","Lower server costs"],answer:1,explanation:"Search engine crawlers receive complete HTML with all content for proper indexing."},{question:"What does hydration mean in SSR?",options:["Running code on server","Attaching event listeners to server HTML","Compressing HTML","Optimizing images"],answer:1,explanation:"Hydration makes server-rendered static HTML interactive by attaching React event handlers."},{question:"What happens if a page exports both getStaticProps and getServerSideProps?",options:["Both run","getServerSideProps takes priority","getStaticProps takes priority","Build error"],answer:3,explanation:"A page cannot export both; it causes a build error."},{question:"How can you cache SSR pages at the CDN level?",options:["Using Cache-Control headers","Using localStorage","Using cookies","Using Redux"],answer:0,explanation:"Set Cache-Control headers like s-maxage and stale-while-revalidate for CDN caching."}]};export{e as nextjs_ssr};
