const t={id:"sql-pivot",title:"PIVOT & Crosstab Queries",difficulty:"advanced",estimatedMinutes:25,tldr:["Pivot queries transform rows into columns, creating crosstab or matrix-style reports from normalized data.","Crosstab queries are necessary for: sales by month (rows=products, columns=months), attendance by department, survey results by category.","PostgreSQL does not have a built-in PIVOT operator. Use tablefunc extension (crosstab function) or conditional aggregation with CASE.","Conditional aggregation (CASE inside aggregate) is the most portable approach, working in all SQL databases."],laymanDefinition:'A pivot is like turning a vertical list into a spreadsheet. Instead of a list like "Product A - Jan - 100, Product A - Feb - 200", you get a table with months as columns: "Product A | Jan: 100 | Feb: 200 | Mar: 150". Rows become columns.',deepDive:[{heading:"Conditional Aggregation (CASE Pivot)",text:"SELECT category, SUM(CASE WHEN month = \\'Jan\\' THEN amount END) AS jan, ... FROM sales GROUP BY category. Most portable — works in all databases. Easy to understand. Column count is fixed at query time."},{heading:"tablefunc Extension (crosstab)",text:"CREATE EXTENSION tablefunc; SELECT * FROM crosstab(\\'source_sql\\', \\'category_sql\\') AS ct(row_name TYPE, col1 TYPE, col2 TYPE, ...). Source query must return (row_name, category, value) ordered correctly. Dynamic columns are not supported."},{heading:"FILTER Clause (PostgreSQL 9.4+)",text:"SELECT category, COUNT(*) FILTER (WHERE month = \\'Jan\\') AS jan, ... FROM sales GROUP BY category. Cleaner syntax than CASE. PostgreSQL-specific. Same performance as CASE."},{heading:"Dynamic Pivot with crosstab + hstore",text:"For unknown columns at query time: use crosstab with hstore or jsonb_agg. SELECT jsonb_object_agg(month, total) FROM ... Returns a JSON object instead of separate columns. Flexible for dynamic reporting."},{heading:"Use Cases",text:"Sales reports: products x months. Employee headcount by department and status. Survey responses by question and rating. Any grid/matrix report where rows and columns come from data."}],interviewAnswer:"Pivoting is essential for reporting and dashboards. Conditional aggregation with CASE is the most portable approach. The crosstab function is faster for large datasets but requires fixed column definitions. For truly dynamic columns, use JSON aggregation.",interviewQuestions:[{question:"What is a pivot query?",answer:"Transforms row values into column headers, creating a crosstab/matrix from normalized data."},{question:"What is the most portable pivot technique?",answer:"Conditional aggregation: SUM(CASE WHEN condition THEN value END). Works in all SQL databases."},{question:"What does the tablefunc extension provide?",answer:"The crosstab() function for pivot queries in PostgreSQL."},{question:"What is the FILTER clause for pivots?",answer:"PostgreSQL syntax: COUNT(*) FILTER (WHERE month = \\'Jan\\'). Cleaner than CASE."},{question:"What are the requirements for crosstab()?",answer:"Source query must return (row_name, category, value) sorted by row_name then category. Column definition must match the output."},{question:"How do you create dynamic pivot columns?",answer:"Use jsonb_object_agg() to aggregate into a JSON object instead of separate columns."},{question:"What is a common pivot use case?",answer:"Monthly sales report: rows = products, columns = months, values = sales amounts."},{question:"Can you pivot with multiple value columns?",answer:"Yes. Use multiple CASE expressions or crosstab() with multiple value columns."},{question:"What is the difference between CASE and FILTER for pivots?",answer:"CASE is portable. FILTER is cleaner syntax but PostgreSQL-only. Performance is similar."},{question:"How do you handle NULLs in pivot results?",answer:"Use COALESCE(SUM(CASE ...), 0) to show 0 instead of NULL for missing combinations."}],diagramSvg:'<svg viewBox="0 0 500 300" xmlns="http://www.w3.org/2000/svg" style="max-width:100%;height:auto;font-family:sans-serif"><defs><marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="8" markerHeight="8" orient="auto"><path d="M0,0 L10,5 L0,10" fill="#666" opacity="0.7"/></marker></defs><rect x="0" y="0" width="500" height="300" rx="10" fill="#f8f9fa" stroke="#dee2e6" stroke-width="1"/><text x="250" y="28" text-anchor="middle" font-size="14" font-weight="bold" fill="#333">PIVOT & Crosstab Queries</text><rect x="10" y="35" width="130" height="25" rx="5" fill="#0070f3" stroke="#0070f3" stroke-width="1.5"/><text x="75" y="51" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">CASE</text><text x="75" y="54" text-anchor="middle" font-size="9" fill="#ddd">Portable pivot</text><rect x="10" y="65" width="130" height="25" rx="5" fill="#28a745" stroke="#28a745" stroke-width="1.5"/><text x="75" y="81" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">FILTER</text><text x="75" y="84" text-anchor="middle" font-size="9" fill="#ddd">Clean syntax</text><rect x="10" y="95" width="130" height="25" rx="5" fill="#ffc107" stroke="#ffc107" stroke-width="1.5"/><text x="75" y="111" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">crosstab()</text><text x="75" y="114" text-anchor="middle" font-size="9" fill="#ddd">Fast pivot</text><rect x="10" y="125" width="130" height="25" rx="5" fill="#dc3545" stroke="#dc3545" stroke-width="1.5"/><text x="75" y="141" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">jsonb_agg</text><text x="75" y="144" text-anchor="middle" font-size="9" fill="#ddd">Dynamic cols</text><line x1="140" y1="48" x2="170" y2="48" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><line x1="140" y1="78" x2="170" y2="78" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><line x1="140" y1="108" x2="170" y2="108" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><line x1="140" y1="138" x2="170" y2="138" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="180" y="35" width="200" height="130" rx="5" fill="#17a2b8" stroke="#17a2b8" stroke-width="1.5"/><text x="280" y="51" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Pivot & Crosstab</text><text x="280" y="148" text-anchor="middle" font-size="9" fill="#ddd">Transform rows to columns for matrix</text><text x="280" y="159" text-anchor="middle" font-size="9" fill="#ddd">-style reports and dashboards.</text><text x="240" y="195" font-size="9" fill="#666" text-anchor="middle">Pivot Queries: Convert rows to columns for crossta</text><text x="240" y="207" font-size="9" fill="#666" text-anchor="middle">b reports using CASE, FILTER, or crosstab().</text></svg>',codeExamples:[{title:"CASE-Based Pivot",useCase:"Monthly sales by product.",code:`SELECT
  product_name,
  SUM(CASE WHEN month = 1 THEN amount ELSE 0 END) AS jan,
  SUM(CASE WHEN month = 2 THEN amount ELSE 0 END) AS feb,
  SUM(CASE WHEN month = 3 THEN amount ELSE 0 END) AS mar,
  SUM(amount) AS total
FROM monthly_sales
WHERE year = 2024
GROUP BY product_name
ORDER BY product_name;`,description:"CASE-based pivoting: months become columns, products are rows."},{title:"FILTER Clause Pivot",useCase:"Cleaner PostgreSQL syntax.",code:`SELECT
  department,
  COUNT(*) FILTER (WHERE status = 'active') AS active,
  COUNT(*) FILTER (WHERE status = 'inactive') AS inactive,
  COUNT(*) FILTER (WHERE status = 'on_leave') AS on_leave,
  COUNT(*) AS total
FROM employees
GROUP BY department
ORDER BY department;`,description:"FILTER clause provides cleaner syntax than CASE for aggregate pivots."},{title:"crosstab() Function",useCase:"Using tablefunc extension.",code:`CREATE EXTENSION IF NOT EXISTS tablefunc;

SELECT * FROM crosstab(
  'SELECT product_name, month, amount'
   FROM monthly_sales
   WHERE year = 2024
   ORDER BY 1, 2',
  'SELECT generate_series(1, 12) AS month'
) AS ct(
  product_name TEXT,
  jan NUMERIC, feb NUMERIC, mar NUMERIC,
  apr NUMERIC, may NUMERIC, jun NUMERIC,
  jul NUMERIC, aug NUMERIC, sep NUMERIC,
  oct NUMERIC, nov NUMERIC, dec NUMERIC
);`,description:"crosstab() function creates the pivot with fixed column definitions."},{title:"Dynamic JSON Pivot",useCase:"Unknown columns at query time.",code:`SELECT
  department,
  jsonb_object_agg(status, count ORDER BY status) AS status_breakdown
FROM (
  SELECT department, status, COUNT(*) AS count
  FROM employees
  GROUP BY department, status
) sub
GROUP BY department
ORDER BY department;

-- Result: department | {"active": 15, "inactive": 3}`,description:"jsonb_object_agg creates flexible pivots when column names are not known in advance."},{title:"Multiple Value Columns",useCase:"Pivot with multiple measures.",code:`SELECT
  department,
  SUM(CASE WHEN status = 'active' THEN 1 ELSE 0 END) AS active_count,
  ROUND(AVG(CASE WHEN status = 'active' THEN salary END), 0) AS active_avg_sal,
  SUM(CASE WHEN status = 'inactive' THEN 1 ELSE 0 END) AS inactive_count
FROM employees
GROUP BY department
ORDER BY department;`,description:"Pivoting multiple measures (count and average) across categories."}],mcqQuestions:[{question:"What does a pivot query do?",options:["Columns to rows","Rows to columns","Sorts data","Filters data"],answer:1,explanation:"Pivot transforms row values into column headers."},{question:"What is the most portable pivot technique?",options:["crosstab()","FILTER clause","CASE aggregation","PIVOT keyword"],answer:2,explanation:"CASE-based conditional aggregation works in all SQL databases."},{question:"What PostgreSQL extension provides crosstab?",options:["tablefunc","pivot","crosstab","matricies"],answer:0,explanation:"The tablefunc extension provides the crosstab() function."},{question:"How do you create dynamic pivot columns?",options:["crosstab()","jsonb_object_agg()","FILTER clause","Multiple CASE"],answer:1,explanation:"jsonb_object_agg creates flexible JSON pivots for dynamic columns."},{question:"What is required for crosstab() source SQL?",options:["Any order","Sorted by row then category","Sorted by value","Grouped"],answer:1,explanation:"The source query must return rows sorted by row_name then category."},{question:"What PostgreSQL syntax is cleaner than CASE for pivots?",options:["PIVOT","FILTER","crosstab","UNPIVOT"],answer:1,explanation:"The FILTER clause provides cleaner aggregate pivot syntax."}]};export{t as sql_pivot};
