const e={id:"react-forward-ref",title:"React forwardRef",difficulty:"intermediate",estimatedMinutes:20,tldr:["forwardRef lets a parent component pass a ref through a child component to one of its DOM elements.","It wraps a function component to accept a ref as the second argument after props.","Essential for reusable component libraries where the parent needs imperative access to internal DOM nodes.","forwardRef is often combined with useImperativeHandle to expose a limited API instead of the raw DOM node."],laymanDefinition:'forwardRef is like giving a child component a forwarding address. Normally, function components do not accept refs (they are not DOM elements). But forwardRef creates a "forwarding address" so the parent can send a ref to a child and have it automatically forwarded to a specific DOM element inside the child. This is useful when you build reusable components like FancyInput or CustomButton - the parent should be able to call .focus() on them, but the actual <input> is inside the child. Without forwardRef, you would need to expose a focus method manually or use prop drilling.',deepDive:[{heading:"How forwardRef Works",text:"forwardRef accepts a render function with two parameters: (props, ref). The component returns JSX normally, but attaches the ref to a specific DOM element (or another component). The ref passed by the parent arrives as the second argument. React handles the ref forwarding automatically during reconciliation: when the parent passes ref={myRef} to the wrapped component, the inner render function receives the ref and can attach it to a child. forwardRef is a higher-order component conceptually - it wraps the inner component and returns a new component that accepts ref. The ref is not a prop - React treats ref specially and does not pass it as props[ref]. forwardRef bridges this gap by making the ref accessible in the function signature."},{heading:"forwardRef with useImperativeHandle",text:'useImperativeHandle customizes the instance value exposed by the forwarded ref. Instead of exposing the raw DOM node, the child defines a limited API object. Example: useImperativeHandle(ref, () => ({ focus: () => inputRef.current.focus(), reset: () => { inputRef.current.value = ""; }, validate: () => inputRef.current.checkValidity() }), []). The parent receives { focus, reset, validate } instead of the raw <input> element. Benefits: encapsulation (parent cannot access arbitrary DOM properties), abstraction (internal implementation can change without affecting parent), and security (expose only safe operations). Use useImperativeHandle sparingly - prefer declarative props over imperative refs when possible.'},{heading:"When to Use forwardRef",text:"(1) Reusable input components - FancyInput, PhoneInput, DatePicker where the parent needs to focus or validate. (2) Animation libraries - GSAP, Framer Motion need direct DOM access to animated elements inside custom components. (3) Scroll management - scrollTo methods on custom list components. (4) Media components - play/pause/seek on custom VideoPlayer. (5) Third-party library integration - D3, Chart.js, Leaflet need DOM nodes. (6) Measuring/positioning - getBoundingClientRect on internal elements. (7) Form libraries - react-hook-form uses refs for input registration. (8) Do NOT use forwardRef when the parent only needs to pass data - use props instead. forwardRef is for imperative access to DOM or component instance methods."},{heading:"forwardRef with TypeScript",text:"TypeScript typing for forwardRef requires careful generic syntax: const FancyInput = React.forwardRef<HTMLInputElement, FancyInputProps>((props, ref) => <input ref={ref} ... />). The first generic parameter is the ref type (HTMLInputElement), the second is the props type. The ref type must match the element type the ref is attached to. For useImperativeHandle, define a handle interface: interface FancyInputHandle { focus(): void; reset(): void; }. Then: React.forwardRef<FancyInputHandle, Props>((props, ref) => { useImperativeHandle(ref, () => ({ focus, reset }), []); ... }). The parent receives typed access: const ref = useRef<FancyInputHandle>(null); ref.current?.focus()."},{heading:"forwardRef with Higher-Order Components",text:"When combining forwardRef with HOCs (connect, withRouter, withStyles), ref forwarding must be manually handled. The HOC typically consumes the ref in its outer component and must forward it using forwardRef. This was a common pain point in class-component era Redux. Modern pattern: Redux Toolkit's connect has a { forwardRef: true } option. For custom HOCs, the pattern is: function withHOC(Component) { return React.forwardRef((props, ref) => <Component {...props} forwardedRef={ref} />); }. The inner component receives ref via forwardedRef prop. Avoid combining multiple HOCs with forwardRef - prefer hooks for cross-cutting concerns."}],interviewAnswer:"forwardRef allows a function component to accept a ref from its parent and forward it to a child DOM element. The component receives (props, ref) as arguments. forwardRef is essential for reusable component libraries where parents need imperative access (focus, scroll, measure). Combine with useImperativeHandle to expose a limited API instead of the raw DOM node. forwardRef supports TypeScript generics for type-safe refs. Avoid forwardRef when declarative props suffice - it is an escape hatch for imperative operations.",interviewQuestions:[{question:"What problem does forwardRef solve?",answer:"Function components do not accept refs by default. forwardRef creates a bridge so the parent can pass a ref through a child component to a specific DOM element inside it. Without forwardRef, the ref prop is ignored on function components."},{question:"How is forwardRef different from passing a ref prop?",answer:"forwardRef uses Reacts built-in ref mechanism (second argument in the function signature). Passing a ref as a regular prop (e.g., <Child innerRef={ref} />) works but is non-standard and loses Reacts ref integration (e.g., callback refs, ref cleanup in React 18). forwardRef is the idiomatic approach."},{question:"What does useImperativeHandle do?",answer:"It customizes the value exposed by the forwarded ref. Instead of exposing the raw DOM node, the child defines a specific API object (e.g., { focus, reset }). This encapsulates the implementation and prevents the parent from accessing arbitrary DOM properties."},{question:"What is the TypeScript syntax for forwardRef?",answer:"React.forwardRef<RefType, PropsType>((props, ref) => ...). RefType is the type of the element or handle (e.g., HTMLInputElement or a custom interface). PropsType is the components props interface."},{question:"Can forwardRef be used with React.memo?",answer:"Yes: React.memo(React.forwardRef(MyComponent)). Both HOCs compose correctly. The memoization compares the props (ref is excluded from comparison). The forwarded ref works normally through the memo wrapper."},{question:"When should you NOT use forwardRef?",answer:"When the parent only needs to pass data or callbacks to the child. Use regular props instead. forwardRef is for imperative operations: focus, scroll, measure, play, seek. For data flow, props are declarative and should be preferred."},{question:"How does forwardRef work with ref callback patterns?",answer:"Callback refs work the same way: the parent passes a callback function to ref, which forwardRef passes to the child. The child calls the callback with the DOM node when it mounts and null when it unmounts."},{question:"What happens if a forwarded ref is not attached to any DOM element?",answer:"The ref.current will be null. This happens if the child does not use the ref or if the component renders conditionally. The parent must check ref.current !== null before accessing properties."},{question:"Can you forward multiple refs from a single component?",answer:"No, forwardRef forwards only one ref. For multiple refs, use multiple wrapper components or pass additional refs via custom prop names (e.g., innerRef, scrollRef). React does not support forwarding multiple refs through a single forwardRef call."},{question:"How do you debug ref forwarding issues?",answer:"Check that: (1) the component is wrapped in forwardRef, (2) the ref is attached to a DOM element (not a function component without forwardRef), (3) the parent creates the ref with useRef(null), (4) the component is mounted (ref.current is null before mount). React DevTools shows which components use forwardRef."}],diagramSvg:'<svg viewBox="0 0 720 280" xmlns="http://www.w3.org/2000/svg" style="max-width:720px;"><defs><marker id="a" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto"><polygon points="0 0,10 3.5,0 7" fill="#6c9fff"/></marker></defs><rect x="10" y="10" width="700" height="260" rx="10" fill="var(--bg-card)" stroke="var(--border)" stroke-width="1"/><text x="360" y="38" fill="#e8eaed" font-size="14" font-weight="bold" text-anchor="middle">forwardRef Flow</text><rect x="30" y="55" width="200" height="45" rx="6" fill="#1a1d28" stroke="#6c9fff" stroke-width="1.5"/><text x="130" y="72.5" fill="#e8eaed" font-size="11" font-weight="bold" text-anchor="middle">Parent</text><text x="130" y="89.5" fill="#9aa0b0" font-size="10" text-anchor="middle">const inputRef = useRef(null)</text><line x1="130" y1="100" x2="130" y2="120" stroke="#6c9fff" stroke-width="2" marker-end="url(#a)"/><rect x="30" y="120" width="200" height="40" rx="6" fill="#1a1d28" stroke="#f59e0b" stroke-width="1.5"/><text x="130" y="135" fill="#e8eaed" font-size="11" font-weight="bold" text-anchor="middle"><FancyInput ref={inputRef}></text><text x="130" y="152" fill="#9aa0b0" font-size="10" text-anchor="middle">forwardRef wrapper</text><line x1="130" y1="160" x2="130" y2="180" stroke="#6c9fff" stroke-width="2" marker-end="url(#a)"/><rect x="30" y="180" width="200" height="45" rx="6" fill="#1a1d28" stroke="#34d399" stroke-width="1.5"/><text x="130" y="197.5" fill="#e8eaed" font-size="11" font-weight="bold" text-anchor="middle"><input ref={ref} /></text><text x="130" y="214.5" fill="#9aa0b0" font-size="10" text-anchor="middle">DOM element receives the ref</text><text x="250" y="78" fill="#9aa0b0" font-size="10" text-anchor="start">useRef creates ref object</text><text x="250" y="135" fill="#9aa0b0" font-size="10" text-anchor="start">forwardRef passes ref as 2nd arg</text><text x="250" y="200" fill="#34d399" font-size="10" text-anchor="start">inputRef.current = actual <input> DOM</text></svg>',codeExamples:[{title:"Reusable FancyInput with forwardRef and useImperativeHandle",useCase:"Expose a focused API, not the raw DOM node",code:`const FancyInput = React.forwardRef((props, ref) => {
  const inputRef = useRef(null);

  useImperativeHandle(ref, () => ({
    focus: () => {
      inputRef.current.focus();
      inputRef.current.style.outline = "2px solid #6c9fff";
    },
    reset: () => { inputRef.current.value = ""; },
    validate: () => inputRef.current.checkValidity()
  }), []);

  return (
    <div className="fancy-input">
      <label>{props.label}</label>
      <input ref={inputRef} {...props} className="input-field" />
    </div>
  );
});

function Form() {
  const emailRef = useRef(null);
  const passwordRef = useRef(null);

  useEffect(() => { emailRef.current.focus(); }, []);

  return (
    <form>
      <FancyInput ref={emailRef} label="Email" type="email" />
      <FancyInput ref={passwordRef} label="Password" type="password" />
      <button type="button" onClick={() => {
        if (!emailRef.current.validate()) emailRef.current.focus();
      }}>Submit</button>
    </form>
  );
}`,description:"The parent receives a clean API (focus, reset, validate) instead of the raw input element. The implementation can change (e.g., switch from <input> to a third-party component) without affecting the parent."},{title:"forwardRef with Animation Library (GSAP)",useCase:"Direct DOM access for animations inside a custom component",code:`const AnimatedCard = React.forwardRef(({ children, ...props }, ref) => {
  return (
    <div ref={ref} className="card" {...props}>
      {children}
    </div>
  );
});

function App() {
  const cardRef = useRef(null);

  useEffect(() => {
    gsap.from(cardRef.current, {
      opacity: 0,
      y: 50,
      duration: 0.6,
      ease: "power3.out"
    });
  }, []);

  return (
    <AnimatedCard ref={cardRef}>
      <h2>Animated Content</h2>
      <p>This card fades in and slides up using GSAP.</p>
    </AnimatedCard>
  );
}`,description:"GSAP needs the actual DOM node to animate. forwardRef provides direct access to the card div inside AnimatedCard. The parent creates the animation imperatively without the child needing to know about GSAP."}],mcqQuestions:[{question:"What does forwardRef do?",options:["Creates a new ref","Allows function components to receive a ref from parent and forward it to a DOM element","Creates a forwarded copy of the component","Prevents refs from being passed to children"],answer:1,explanation:"forwardRef wraps a function component so it receives ref as the second argument, enabling ref forwarding to a child DOM element."},{question:"How many parameters does a forwardRef render function receive?",options:["One: props","Two: (props, ref)","Three: (props, ref, context)","Four: (props, ref, context, owner)"],answer:1,explanation:"The render function inside forwardRef receives props as the first argument and ref as the second argument."},{question:"What hook customizes the value exposed by a forwarded ref?",options:["useRef","useImperativeHandle","useCallback","useMemo"],answer:1,explanation:"useImperativeHandle(ref, createHandle, deps) defines the object that the parent receives when accessing ref.current."},{question:"Can forwardRef be used with class components?",options:["Yes, class components also need forwardRef","No, forwardRef is only for function components (class components already accept refs)","Only with PureComponent","Only with TypeScript"],answer:1,explanation:"Class components already support refs natively via the instance. forwardRef is specifically for function components that cannot accept refs directly."},{question:"What is the second generic parameter in React.forwardRef<RefType, PropsType>?",options:["The ref type","The props type","The return type","The children type"],answer:1,explanation:"The first generic is the ref (element or handle) type. The second generic is the props type. Example: React.forwardRef<HTMLInputElement, { label: string }>."},{question:"Which is NOT a valid use case for forwardRef?",options:["Focus management on a custom input","Passing data to a child component","Integrating with animation libraries","Measuring DOM element dimensions"],answer:1,explanation:"forwardRef is for imperative operations (focus, measure, animate). Data passing should use declarative props."}]};export{e as react_forward_ref};
