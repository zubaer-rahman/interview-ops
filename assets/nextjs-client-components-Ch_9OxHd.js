const e={id:"nextjs-client-components",title:"Client Components",difficulty:"intermediate",estimatedMinutes:25,tldr:["Client Components are React components that render on the client browser, enabling interactivity through hooks, event handlers, and browser APIs.",'Marked with the "use client" directive at the top of the file, creating a clear boundary between server and client code.','All components imported into a Client Component file also become part of the client bundle, making the placement of the "use client" directive strategically important.',"Client Components can be rendered server-side during the initial page load (SSR) but are hydrated and become interactive on the client."],laymanDefinition:"Client Components are like interactive touchscreens in a museum: the content may be prepared by the server, but the buttons, animations, and real-time updates all happen on your device.",deepDive:[{heading:'The "use client" Directive',text:'Adding "use client" at the top of a file marks it and all its imports as Client Components. This directive creates the server-client boundary. All React hooks (useState, useEffect, useRef, useCallback, useMemo, useContext) and event handlers are only available in Client Components.'},{heading:"Server-Side Rendering of Client Components",text:"Client Components are pre-rendered on the server during initial page load, producing static HTML. After the JavaScript loads, React hydrates the component, making it interactive. This means Client Components get both SSR benefits (fast initial HTML) and client interactivity."},{heading:"Bundle Size Considerations",text:'Client Components increase the JavaScript bundle size. Strategically place the "use client" directive at the lowest possible level to minimize the client bundle. Server Components that contain no client logic should remain server-only. Use dynamic imports with ssr: false for rarely-used client components.'},{heading:"Composition Patterns",text:"Best practice: Server Components handle data fetching and static rendering, passing data as props to Client Components. Client Components handle interactivity. Use the children prop pattern to pass Server Component output into Client Component wrappers. Avoid importing Server Components into Client Components."},{heading:"Third-Party Library Usage",text:"Many React libraries (UI components, charts, form libraries) require client-side JavaScript. Wrap third-party client libraries in Client Components. Server Components can import and render these client wrappers. Some newer libraries offer both server and client exports."}],interviewAnswer:'Client Components are essential for interactive web applications but should be used strategically. The recommended pattern is maximal Server Components with minimal, targeted Client Components. Each "use client" boundary should be justified by actual interactivity requirements.',interviewQuestions:[{question:"What are Client Components in Next.js?",answer:'Client Components are components that render on the client browser. They support React hooks, event handlers, browser APIs, and state management. They are marked with the "use client" directive and can be server-side rendered during initial load for SEO benefits.'},{question:"How do you create a Client Component?",answer:'Add "use client" as the first line of the file. This marks the component and all its direct imports as client-side. Then you can use React hooks, event handlers, and browser APIs. The component will be pre-rendered on the server and hydrated on the client.'},{question:"What is the server-client boundary?",answer:'The server-client boundary is defined by the "use client" directive. Everything above the boundary (server-side) is rendered on the server. Everything below the boundary (the Client Component and its imports) is rendered on the client. This boundary determines what code goes into the client bundle.'},{question:"Can Client Components be server-side rendered?",answer:"Yes, Client Components are pre-rendered on the server during the initial page load. This produces static HTML that is sent to the client. After the JavaScript bundle loads, React hydrates the HTML, attaching event handlers and making it interactive."},{question:"How do you minimize client bundle size with Client Components?",answer:'Place the "use client" directive at the lowest possible component level. Keep data fetching and static rendering in Server Components. Use dynamic imports with next/dynamic for client components that are not immediately visible. Pass Server Components as children to Client Components using the children prop.'},{question:"What happens when you import a Server Component into a Client Component?",answer:"If you import a Server Component directly into a Client Component, the Server Component becomes a Client Component too (it loses its server-side capabilities). Use the composition pattern: pass Server Components as children or props to Client Components instead of importing them."},{question:"How do you use third-party UI libraries in the App Router?",answer:'Third-party UI components that use hooks or browser APIs must be wrapped in Client Components. Create a wrapper file with "use client", import the library, and re-export it. Server Components can then import and render this client wrapper.'},{question:"What hooks are available in Client Components?",answer:"All React hooks: useState, useEffect, useRef, useCallback, useMemo, useReducer, useContext, useImperativeHandle, useLayoutEffect, useDebugValue, useId, useSyncExternalStore, useTransition, useDeferredValue. Custom hooks that use these hooks are also available."},{question:"Can Client Components use environment variables?",answer:"Client Components can only access environment variables prefixed with NEXT_PUBLIC_. Server-only environment variables are not available in Client Components. Use NEXT_PUBLIC_ prefix for values that need to be accessed client-side."},{question:"How do you handle loading states in Client Components?",answer:"Use React hooks like useState for loading state, useEffect for data fetching with loading indicators, or libraries like SWR/TanStack Query that provide built-in loading states. For suspense-based loading, wrap Client Components in Suspense boundaries defined in Server Components."}],diagramSvg:'<svg viewBox="0 0 500 200" xmlns="http://www.w3.org/2000/svg" style="max-width:100%;height:auto;font-family:sans-serif"><rect x="0" y="0" width="500" height="200" rx="8" fill="#f8f9fa" stroke="#dee2e6" stroke-width="1"/><text x="250" y="24" text-anchor="middle" font-size="14" font-weight="bold" fill="#333">Client Components</text><rect x="10" y="40" width="120" height="35" rx="4" fill="#0070f3" stroke="#0070f3" stroke-width="1"/><text x="70" y="56" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">"use client"</text><text x="70" y="68" text-anchor="middle" font-size="9" fill="#ddd">Directive</text><line x1="130" y1="58" x2="160" y2="58" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="170" y="40" width="120" height="35" rx="4" fill="#28a745" stroke="#28a745" stroke-width="1"/><text x="230" y="56" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Client Bundle</text><text x="230" y="68" text-anchor="middle" font-size="9" fill="#ddd">Includes JS</text><line x1="290" y1="58" x2="320" y2="58" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="330" y="40" width="120" height="35" rx="4" fill="#ffc107" stroke="#ffc107" stroke-width="1"/><text x="390" y="56" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Hydration</text><text x="390" y="68" text-anchor="middle" font-size="9" fill="#ddd">on Client</text><line x1="330" y1="75" x2="330" y2="95" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="230" y="105" width="130" height="35" rx="4" fill="#17a2b8" stroke="#17a2b8" stroke-width="1"/><text x="295" y="121" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Interactive</text><text x="295" y="133" text-anchor="middle" font-size="9" fill="#ddd">useState/onClick</text><rect x="10" y="105" width="130" height="35" rx="4" fill="#6610f2" stroke="#6610f2" stroke-width="1"/><text x="75" y="121" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Server Pre-render</text><text x="75" y="133" text-anchor="middle" font-size="9" fill="#ddd">Initial HTML</text><text x="250" y="170" font-size="9" fill="#666" text-anchor="middle">Client Components: Browser-rendered with hooks, interactivity, and browser API access.</text></svg>',codeExamples:[{title:"Basic Client Component with State",useCase:"When you need a counter or toggle.",code:`"use client";
import { useState } from "react";

export default function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>Count: {count}</button>
}`,description:"A simple interactive counter using useState hook."},{title:"Client Component with useEffect",useCase:"When you need browser-side data fetching.",code:`"use client";
import { useState, useEffect } from "react";

export default function GeoLocation() {
  const [location, setLocation] = useState(null);
  useEffect(() => {
    navigator.geolocation.getCurrentPosition(pos => setLocation(pos.coords));
  }, []);
  return <div>Lat: {location?.latitude}, Lng: {location?.longitude}</div>;
}`,description:"Uses the browser Geolocation API, which requires client-side execution."},{title:"Client Component with Server Component Composition",useCase:"When you want to minimize client code.",code:`// Server Component (parent)
import InteractivePanel from "./InteractivePanel";

export default async function Dashboard() {
  const data = await fetchDashboardData();
  return (
    <div>
      <h1>Dashboard</h1>
      <InteractivePanel initialData={data} />
    </div>
  );
}

// Client Component (child)
"use client";
export default function InteractivePanel({ initialData }) {
  const [filtered, setFiltered] = useState(initialData);
  return <div onClick={() => setFiltered(initialData.filter(f))}>...</div>
}`,description:"Server Component fetches data, Client Component handles interactivity."},{title:"Dynamic Import of Client Component",useCase:"When you want to lazy-load a heavy client component.",code:`import dynamic from "next/dynamic";

const HeavyChart = dynamic(() => import("../components/Chart"), {
  ssr: false,
  loading: () => <div>Loading chart...</div>
});

export default function Page() {
  return <HeavyChart />;
}`,description:"The Chart component is loaded only when needed, reducing initial bundle size."},{title:"Third-Party Library Wrapper",useCase:"When using a React library that requires client-side execution.",code:`"use client";
import { motion } from "framer-motion";

export default function AnimatedElement({ children }) {
  return <motion.div animate={{ opacity: 1 }}>{children}</motion.div>;
}`,description:"Wraps framer-motion in a Client Component so it can be used from a Server Component."}],mcqQuestions:[{question:"What directive marks a component as a Client Component?",options:['"use server"','"use client"','"use browser"','"use csr"'],answer:1,explanation:'The "use client" directive marks the server-client boundary.'},{question:"Which feature is available in Client Components but NOT in Server Components?",options:["Database queries","Environment variables","useState hook","Direct data fetching"],answer:2,explanation:"React hooks like useState are only available in Client Components."},{question:"What happens to Client Components during initial page load?",options:["They are not rendered","They are pre-rendered as HTML on the server","They only render after hydration","They require JavaScript to show anything"],answer:1,explanation:"Client Components are pre-rendered on the server for initial HTML, then hydrated."},{question:"How should you minimize the client bundle?",options:["Use many Client Components",'Place "use client" at the leaf level only',"Avoid using hooks","Use inline scripts"],answer:1,explanation:'Place "use client" at the lowest possible component level to minimize client bundle size.'},{question:"What happens when you import a Server Component directly into a Client Component?",options:["It stays a Server Component","It becomes part of the client bundle","It throws an error","It renders on both sides"],answer:1,explanation:"Directly imported Server Components become part of the client bundle and lose server capabilities."},{question:"Which environment variables can Client Components access?",options:["All environment variables","Only NEXT_PUBLIC_ prefixed variables","Only server variables","No environment variables"],answer:1,explanation:"Client Components can only access environment variables with the NEXT_PUBLIC_ prefix."}]};export{e as nextjs_client_components};
