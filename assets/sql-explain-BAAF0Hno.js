const e={id:"sql-explain",title:"Query Optimization & EXPLAIN",difficulty:"advanced",estimatedMinutes:30,tldr:["EXPLAIN shows the query execution plan — how the database will execute a query, including index usage, join methods, and cost estimates.","EXPLAIN ANALYZE actually executes the query and shows actual vs estimated times. Use with caution on write queries.","Key plan nodes: Seq Scan (full table scan), Index Scan (index lookup), Index Only Scan (all data in index), Bitmap Heap Scan.","Optimization: index creation, query rewriting, avoiding full scans, proper join order, and analyzing table statistics."],laymanDefinition:"EXPLAIN is like looking at a GPS route before driving. It shows the roads (scans), the turns (joins), and the estimated time (cost). EXPLAIN ANALYZE is like actually driving the route and recording the real time — it tells you where you got stuck in traffic.",deepDive:[{heading:"EXPLAIN Output",text:"Seq Scan: scans entire table (slowest, but sometimes fastest for small tables). Index Scan: uses index to find rows (fast for selective queries). Index Only Scan: reads only index, no table access (fastest). Bitmap Heap Scan: combines multiple index results."},{heading:"Cost Values",text:"Format: cost=0.00..42.35 rows=10 width=4. First number: startup cost before first row. Second: total cost to return all rows. rows: estimated row count. width: estimated average row width in bytes. Compare costs between different query plans."},{heading:"Join Methods",text:"Nested Loop: for each row in outer, scan inner (good for small outer + indexed inner). Hash Join: build hash table on one input, probe with other (good for large, unsorted data). Merge Join: sort both inputs then merge (good for large, pre-sorted data)."},{heading:"ANALYZE and Statistics",text:"ANALYZE table_name; — updates table statistics (row count, value distribution). The query planner relies on accurate statistics. autovacuum runs ANALYZE automatically. pg_stat_user_tables shows last analyze time."},{heading:"Common Optimization Patterns",text:"Add missing indexes on WHERE/JOIN/ORDER BY columns. Rewrite OR conditions as UNION ALL. Use EXISTS instead of DISTINCT for existence checks. Avoid functions on indexed columns in WHERE (e.g., WHERE LOWER(name) = — use expression index)."}],interviewAnswer:"EXPLAIN is the most important tool for query optimization. Understanding execution plans tells you exactly why a query is slow and what to fix. Always check EXPLAIN ANALYZE before and after making optimization changes.",interviewQuestions:[{question:"What does EXPLAIN do?",answer:"Shows the query execution plan: how the database will access tables, which indexes it uses, and cost estimates."},{question:"What is the difference between EXPLAIN and EXPLAIN ANALYZE?",answer:"EXPLAIN shows only the estimated plan. EXPLAIN ANALYZE executes the query and shows actual timing and row counts."},{question:"What is a Sequential Scan?",answer:"Reading every row of a table one by one. Fast for small tables, slow for large tables. Indicates missing indexes."},{question:"What is an Index Scan?",answer:"Using an index to find rows. Fast for selective queries (returning a small percentage of rows)."},{question:"What is an Index Only Scan?",answer:"Reading only the index without accessing the table. Fastest scan type — requires a covering index."},{question:"What is a Nested Loop Join?",answer:"For each row in the outer table, scan the inner table for matches. Best for small outer + indexed inner."},{question:"What is a Hash Join?",answer:"Build a hash table on one input, probe it with the other. Best for large, unsorted data with equality conditions."},{question:"What is a Merge Join?",answer:"Sort both inputs then merge. Best for large, pre-sorted datasets or when ORDER BY is already needed."},{question:"What do cost numbers in EXPLAIN mean?",answer:"cost=X..Y: X is startup cost, Y is total cost. Higher cost = slower plan. Compare different plans."},{question:"How do you update statistics?",answer:"ANALYZE table_name; Updates table statistics for the query planner. PostgreSQL runs autovacuum/autoanalyze automatically."}],diagramSvg:'<svg viewBox="0 0 500 300" xmlns="http://www.w3.org/2000/svg" style="max-width:100%;height:auto;font-family:sans-serif"><defs><marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="8" markerHeight="8" orient="auto"><path d="M0,0 L10,5 L0,10" fill="#666" opacity="0.7"/></marker></defs><rect x="0" y="0" width="500" height="300" rx="10" fill="#f8f9fa" stroke="#dee2e6" stroke-width="1"/><text x="250" y="28" text-anchor="middle" font-size="14" font-weight="bold" fill="#333">Query Optimization & EXPLAIN</text><rect x="10" y="35" width="110" height="25" rx="5" fill="#0070f3" stroke="#0070f3" stroke-width="1.5"/><text x="65" y="51" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">EXPLAIN</text><text x="65" y="54" text-anchor="middle" font-size="9" fill="#ddd">Estimated plan</text><rect x="10" y="65" width="110" height="25" rx="5" fill="#28a745" stroke="#28a745" stroke-width="1.5"/><text x="65" y="81" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">EXPLAIN ANALYZE</text><text x="65" y="84" text-anchor="middle" font-size="9" fill="#ddd">Actual timing</text><rect x="10" y="95" width="110" height="25" rx="5" fill="#ffc107" stroke="#ffc107" stroke-width="1.5"/><text x="65" y="111" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Seq Scan</text><text x="65" y="114" text-anchor="middle" font-size="9" fill="#ddd">Full table</text><rect x="10" y="125" width="110" height="25" rx="5" fill="#dc3545" stroke="#dc3545" stroke-width="1.5"/><text x="65" y="141" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Index Scan</text><text x="65" y="144" text-anchor="middle" font-size="9" fill="#ddd">Fast lookup</text><rect x="10" y="155" width="110" height="25" rx="5" fill="#e83e8c" stroke="#e83e8c" stroke-width="1.5"/><text x="65" y="171" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Hash Join</text><text x="65" y="174" text-anchor="middle" font-size="9" fill="#ddd">Merge large</text><line x1="120" y1="48" x2="150" y2="48" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><line x1="120" y1="78" x2="150" y2="78" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><line x1="120" y1="108" x2="150" y2="108" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><line x1="120" y1="138" x2="150" y2="138" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><line x1="120" y1="168" x2="150" y2="168" stroke="#666" stroke-width="1.5" marker-end="url(#arrow)"/><rect x="160" y="35" width="220" height="155" rx="5" fill="#17a2b8" stroke="#17a2b8" stroke-width="1.5"/><text x="270" y="51" text-anchor="middle" font-size="11" font-weight="bold" fill="#fff">Query Optimization</text><text x="270" y="173" text-anchor="middle" font-size="9" fill="#ddd">Understand execution plans, indexes, joi</text><text x="270" y="184" text-anchor="middle" font-size="9" fill="#ddd">n methods, and statistics.</text><text x="240" y="220" font-size="9" fill="#666" text-anchor="middle">EXPLAIN & Optimization: Understand execution plans</text><text x="240" y="232" font-size="9" fill="#666" text-anchor="middle"> to write faster SQL queries.</text></svg>',codeExamples:[{title:"Reading EXPLAIN Output",useCase:"Understanding a basic plan.",code:`EXPLAIN SELECT * FROM employees WHERE salary > 80000;

-- Output:
-- Seq Scan on employees  (cost=0.00..324.50 rows=15 width=72)
--   Filter: (salary > 80000)

-- After adding an index:
CREATE INDEX ON employees(salary);

-- Output:
-- Index Scan using employees_salary_idx on employees  (cost=0.28..8.29 rows=15 width=72)
--   Index Cond: (salary > 80000)`,description:"Shows how adding an index changes Seq Scan to Index Scan with lower cost."},{title:"EXPLAIN ANALYZE",useCase:"Actual vs estimated.",code:`EXPLAIN ANALYZE
SELECT e.name, d.department_name
FROM employees e
JOIN departments d ON e.dept_id = d.id
WHERE d.department_name = 'Engineering';

-- Shows actual time, rows, loops
-- Look for: actual time vs estimated time mismatch
-- Look for: "rows" vs "rows" mismatch (stale statistics)`,description:"EXPLAIN ANALYZE shows actual execution metrics to compare with estimates."},{title:"Optimizing a Slow Query",useCase:"Step-by-step optimization.",code:`-- Slow query:
EXPLAIN ANALYZE SELECT * FROM orders
WHERE status = 'pending' AND amount > 1000
ORDER BY created_at DESC;

-- Add composite index:
CREATE INDEX idx_orders_status_amount_date
ON orders(status, amount, created_at DESC);

-- Now: Index Scan with index condition, no separate sort
-- Much faster for this query pattern`,description:"A covering composite index can turn a slow query into an instant one."},{title:"Detecting Missing Indexes",useCase:"Using pg_stat_user_tables.",code:`-- Find tables with most sequential scans
SELECT
  schemaname, relname,
  seq_scan, seq_tup_read,
  idx_scan, idx_tup_fetch
FROM pg_stat_user_tables
ORDER BY seq_scan DESC
LIMIT 10;`,description:"Identifies tables where sequential scans dominate — candidates for indexing."},{title:"Optimizing JOINs",useCase:"Rewrite for better performance.",code:`-- Instead of:
SELECT DISTINCT c.name
FROM customers c
JOIN orders o ON c.id = o.customer_id
WHERE o.status = 'shipped';

-- Better:
SELECT c.name
FROM customers c
WHERE EXISTS (
  SELECT 1 FROM orders o
  WHERE o.customer_id = c.id AND o.status = 'shipped'
);

-- EXISTS can early-exit on first match`,description:"EXISTS is often faster than DISTINCT for existence checks."}],mcqQuestions:[{question:"What does EXPLAIN show?",options:["Query results","Execution plan","Table data","Index contents"],answer:1,explanation:"EXPLAIN shows the query execution plan."},{question:"What scan type is fastest?",options:["Seq Scan","Index Scan","Index Only Scan","Bitmap Scan"],answer:2,explanation:"Index Only Scan reads only the index, no table access."},{question:"What does EXPLAIN ANALYZE do?",options:["Shows estimated plan only","Executes query and shows actuals","Shows indexes only","Shows table statistics"],answer:1,explanation:"EXPLAIN ANALYZE executes the query and shows actual timing."},{question:"Which join is best for large unsorted data?",options:["Nested Loop","Hash Join","Merge Join","Cross Join"],answer:1,explanation:"Hash Join is best for large, unsorted datasets with equality joins."},{question:"What does cost=X..Y mean?",options:["X=total, Y=startup","X=startup, Y=total","X=index, Y=scan","X=rows, Y=width"],answer:1,explanation:"X is startup cost before first row, Y is total cost."},{question:"How do you update planner statistics?",options:["UPDATE STATS","ANALYZE","VACUUM","REINDEX"],answer:1,explanation:"ANALYZE updates table statistics for the query planner."}]};export{e as sql_explain};
